
%h4= "#{t('post.title')}: #{@post.title}"
%h4= "#{t('post.subtitle')}: #{@post.subtitle}"
%h4= t('post.description')
= @post.description
%h4= "#{t('post.published_at')}: #{@post.published_at}"

%p
= link_to_edit @slide, :class => 'edit_slide'
= t(:or)
= link_to t(:back), collection_url
